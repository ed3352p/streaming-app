# ============================================
# Configuration Lumixar - Template
# ============================================
# Copiez ce fichier en .env et modifiez les valeurs

# URL de l'API Backend
# Pour VPS: https://votre-domaine.com/api
# Pour développement local: http://localhost:3001/api
VITE_API_URL=http://localhost:3001/api

# Port du serveur backend
PORT=3001

# Environnement (development ou production)
NODE_ENV=development

# Clé secrète JWT (IMPORTANT: Générer une clé unique)
# Générer avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=changez_cette_cle_par_une_cle_aleatoire_forte_minimum_64_caracteres

# Durée de session (en secondes)
# 24h = 86400, 7j = 604800, 30j = 2592000
SESSION_DURATION=86400

# Base de données (optionnel - si vous utilisez une DB)
# DATABASE_URL=mongodb://localhost:27017/lumixar
# DATABASE_URL=postgresql://user:password@localhost:5432/lumixar

# Configuration CORS
# Domaines autorisés (séparés par des virgules)
CORS_ORIGIN=http://localhost:5173,http://localhost:8080,http://localhost:3001

# Limite de taille des uploads (en MB)
MAX_FILE_SIZE=100

# Logs
LOG_LEVEL=info

# Email (pour notifications - optionnel)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=votre-mot-de-passe-app
# EMAIL_FROM=noreply@lumixar.com

# Stripe (paiements - optionnel)
# STRIPE_PUBLIC_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Cloudflare (CDN - optionnel)
# CLOUDFLARE_API_TOKEN=...
# CLOUDFLARE_ZONE_ID=...

# Analytics (optionnel)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Sécurité
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100
